# amp-guitar

Web App that provides tools for guitarists. Built completely in React.js and hosted via github pages.

To deploy:
Save all changes.
Run the build command: npm run build
Push changes to main branch on git.
After a few mins the github action should handle the rest.
